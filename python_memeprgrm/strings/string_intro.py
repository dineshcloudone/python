
import pdb
"""

String is a simple datatype, also compound datatype, sequence datatype and immutable datatype

"""


#### String is immutable
#breakpoint()
string = 'string'
string[1] = 'T' ## Error doesn't support item assignment
new_string= string.replace('t','T')
print(new_string)
